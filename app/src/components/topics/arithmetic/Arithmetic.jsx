import React from "react";
import { useNavigate } from "react-router-dom";

const Arithmetic = () => {
    const navigate = useNavigate();

    return (
        <div>Arithmetic Screen</div>
    )
}

export default Arithmetic;